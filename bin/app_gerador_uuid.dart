import 'package:app_gerador_uuid/app_gerador_uuid.dart' as app_gerador_uuid;

void main(List<String> arguments) {
  app_gerador_uuid.geradorUuid();
  // app_gerador_uuid.geradorDataHoraFormatada();
  // app_gerador_uuid.geradorHoraFormatada();
  // app_gerador_uuid.geradorDataFormatada();
}
